/**
 * JavaScript Functions
 * 
 * Functions are a way of packaging functionality 
 * that you wish to reuse. 
 */

// Define a function that calculates square from the number it recieves as an argument
function square(num) {
    return num * num;
}

// Use the function
var result = square(5);

// Display data
console.log(result);

console.log(square(6));